package game.web.controller;

/**
 * Place any dev/test controller code in this class.
 *
 * Currently unused, but if this file is deleted and a new exploded war is deployed,
 * Tomcat/spring will fail to start. Leave here until we can safely wipe the exploded
 * war directory.
 */

public class DeveloperController { }